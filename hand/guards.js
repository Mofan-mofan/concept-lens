// 同形词护栏 —— 手改文件。
// 问题：金融汉语里大量词是「日常词的借用」。《概念账本》第十节第 5 条就写了：
// 金融词大量同形，必须带领域标签才敢匹配。
//
// 一条实测教训（2026-09-13 第一版）：我一开始给 13 个词配了 require（命中处附近
// 必须出现某些行业词才算数），结果挡掉 52 处、其中 49 处是**真命中**——
// 「退出通道」「良率爬坡」「模组环节」「主键」全都因为周围 24 字内没凑齐那几个
// 提示词而被误杀。语料本身全是金融与半导体文本，require 型护栏在这里是净伤害。
// 所以现在的规则：**只用 exclude 挡具体的假朋友**，不做「必须像才放行」的白名单。
// 将来若把非金融文档纳进语料，再按词的实测误标率逐条加 require。
//
// 格式必须是 window.QD_GUARDS = { …… }; 且对象部分是合法 JSON（注释别写进对象里）。
// 字段：exclude 命中处前后 ±ctx 字内出现它就作废（正则）/ ctx 左右各看几字，默认 12 /
//       why 为什么加这条，写给人看的
window.QD_GUARDS = {
  "颗粒": {"exclude": "(度|纳米)", "ctx": 3,
           "why": "「颗粒度」是颗粒感的比喻、「纳米颗粒」是材料形貌，都不是存储里的「颗粒」（die）"},
  "退出": {"exclude": "(登录|登出|群聊|会议|聊天)", "ctx": 4,
           "why": "只挡软件语境的退出；「退出通道/退出测算/难退出」都是本概念，不能挡"},
  "模组": {"exclude": "(封装|相机|镜头|紫外|红外)", "ctx": 3,
           "why": "B1 的「模组」＝买颗粒卖成品的存储模组厂；氧化镓那几篇里「模组封装」是赛道环节枚举、「代理进口相机／模组」是相机模组、「深紫外模组」是探测器组件，都是同一个汉字的另一个意思。「模组环节」「模组厂」「存储模组」不带这几个词，不会被挡"}
};
